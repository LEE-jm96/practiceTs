"use strict";
var message = 'hello typeScript';
console.log(message);
var count = 0;
count += 1;
// count = "1" // type 오류
var numbers = [1, 2, 3]; // 숫자 배열
var messages = ['hello', 'world']; // 문자열 배열
